import java.awt.image.BufferedImage;
import java.io.File;
import java.io.IOException;

import javax.imageio.ImageIO;


public class ImgHist {
	
	private BufferedImage myImage;
	private File imgFile;
	private byte[][] imgMatrix;
	
	//constructor
	public ImgHist(String fileName) throws IOException{
		this.imgFile = new File(fileName);
		this.myImage = ImageIO.read(imgaFile);
		this.imgMatrix = new byte[myImage.getWidth()][];
		
		for (int i = 0; x < myImage.getWidth(); i++){
			this.imgMatrix[i] = new byte[myImga]
		}
		
	}
	
	//return's byte matrix array with pixel values
	public byte getMatrix(){
		
		return 0;
	}
	
	
	
}
